/** Discrete named upgrades available inside a single tier. */
export const LEVELS_PER_TIER = 9000;

/** Geometric growth. Matched by milestone multipliers so grind stays in band. */
export const LEVEL_COST_RATE = 1.07;

/** Each purchased tier multiplies that column's power. */
export const TIER_POWER_RATE = 2.4;

export const TICK_HZ = 20;
export const WAVE_PERIOD = 7;
export const OFFLINE_CAP_SECONDS = 8 * 60 * 60;
export const STARTING_GOLD = 12;
export const SAVE_KEY = 'canon-lane-v1';

export const WIDTH = 720;
export const HEIGHT = 1280;
