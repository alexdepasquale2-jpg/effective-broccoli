{
  "name": "aether",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 5173",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "test": "vitest run"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
